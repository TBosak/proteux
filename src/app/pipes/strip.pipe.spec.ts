/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { StripPipe } from './strip.pipe';

describe('Pipe: Stripe', () => {
  it('create an instance', () => {
    let pipe = new StripPipe();
    expect(pipe).toBeTruthy();
  });
});
